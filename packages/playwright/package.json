{
  "name": "@ff-mcp/playwright",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright MCP Server deployment package",
  "scripts": {
    "start": "npx @playwright/mcp@latest --port ${PORT:-8080} --host 0.0.0.0 --allowed-hosts '*' --output-dir /app/artifacts --browser chromium --snapshot-mode none --caps devtools --isolated"
  },
  "dependencies": {
    "@playwright/mcp": "latest"
  }
}
